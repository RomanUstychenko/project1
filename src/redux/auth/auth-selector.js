export const isLogin = ({ auth }) => auth.isLogin;
export const isRegister = ({ auth }) => auth.isRegister;
export const getUser = ({ auth }) => auth.newUser;
export const getLoadingUserStatus = ({ auth }) => auth.loading;
